var album = {
  'id': "<%= @album.id  %>",
  'locale': "<%= I18n.locale  %>",
  'title': "<%= @album.title  %>"
};

Albums.addLinks(album);
Albums.addToDropdown(album);
Albums.clearInput('album_title');
