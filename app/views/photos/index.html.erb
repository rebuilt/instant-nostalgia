<div id="modal" class="modal" data-controller="modal"> </div>

<div class="content-wrapper">
  <h1>Photos</h1>

  <div class="gallery">
    <% @photos.each do |photo|   %>
      <%= render(partial: "editable_photo_card", locals: {photo: photo})  %>
    <% end %>
  </div>
</div>
