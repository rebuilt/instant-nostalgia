var comment = {
  "id": "id-<%= @comment.id  %>",
  "count": " <%= @count  %> "
}

Comments.removeComment(comment);
Comments.updateCommentCount(comment, 'comment-count-1');
Comments.updateCommentCount(comment, 'comment-count-2');
