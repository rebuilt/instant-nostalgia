var comment = {
  "id": "id-<%= @comment.id  %>",
  "count": " <%= @count  %> "
}

Helpers.removeElement(comment.id);
Comments.updateCommentCount(comment.count, 'comment-count-1');
Comments.updateCommentCount(comment.count, 'comment-count-2');
