<% if(logged_in? && current_user == album.user)  %>
  <%= link_to 'Share with a user', new_share_path(album_id: album.id),  class: 'btn btn--primary no-radius mr-1', title: "Share this albums with a specific person.  This makes the album visible only to the people you chose to share with." %>
<% end %>
