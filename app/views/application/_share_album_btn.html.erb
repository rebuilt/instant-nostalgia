<% if(logged_in? && current_user == album.user)  %>
  <%= link_to t('global.buttons.share_with'), new_share_path(album_id: album.id),  class: 'btn btn--primary no-radius mr-1', title: t('global.buttons.share_with_title')%>
<% end %>
