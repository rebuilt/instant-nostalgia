<% if(logged_in? && current_user == album.user)   %>
  <%= link_to t('global.buttons.delete_album'), album, method: :delete, class: "btn btn--warning no-radius mr-1", data: { confirm: 'Are you sure?'}, remote: true ,title: t('global.buttons.delete_album_title')%>
<% end %>
